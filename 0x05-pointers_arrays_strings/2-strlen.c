#include "main.h"
#include <string.h>
/**
 * _strlen - finds length of string
 * a: stores length of string
 * @s: pass a string
 * Return: returns string length.
 */

int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
